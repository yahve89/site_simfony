{% extends 'base.html.twig' %}

{% block header %}

    <div class="text-center py-5">
        <h1 class="mt-2">{{ page.title }}</h1>
    </div>

{% endblock %}

{% block body %}
    
    {% for section in blocks %}
        {%  include 'partal/' ~ section.template ~'.html.twig' %}
    {% endfor %}
    
{% endblock %}
